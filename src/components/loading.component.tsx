function Loading() {
    return (
        <div>
            <p className="text-2xl">Loading weather data...</p>
            <p>Weather app should appear once weather data is loaded</p>
        </div>
    );
}

export default Loading;